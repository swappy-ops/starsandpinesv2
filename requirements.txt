# Stars & Pines V2 — Dependencies
# Raw SQLite, no ORM. FastAPI for HTTP.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# SQLite is built-in (sqlite3 module)

# Utilities
python-dotenv==1.0.1
pydantic==2.10.4

# Testing
pytest==8.3.4
httpx==0.28.1          # for FastAPI test client

# Backup / ops
schedule==1.2.2        # for nightly backup cron
